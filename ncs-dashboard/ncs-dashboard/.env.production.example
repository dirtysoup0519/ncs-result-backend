# 生产/答辩部署建议显式关闭 Mock。
VITE_USE_MOCK=false
VITE_DEMO_DATA=false

# 若与 Flask 由同一反向代理域名提供，可保持 /api/v1。
VITE_API_BASE_URL=/api/v1

# 仅在 loadPrediction 正式启用后，由算法/后端共同确认。
# VITE_PREDICTION_DATE=2019-09-13
# VITE_PREDICTION_CUTOFF_HOUR=16
